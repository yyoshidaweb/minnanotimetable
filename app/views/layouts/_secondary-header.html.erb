<%# セカンドヘッダー共通テンプレート %>
<% if defined?(@page_title) && @page_title.present? %>
  <header class=" mt-12 fixed top-0 left-0 w-full h-8 bg-white border-b border-gray-100 z-50 flex items-center">
    <div class="w-full mx-auto px-4 sm:px-6 lg:px-8 flex justify-between items-center">
      <p></p>
      <h2 class="text-sm text-gray-800"><%= @page_title %></h2>
      <p></p>
    </div>
  </header>
  <div class="h-8"></div>
<% end %>